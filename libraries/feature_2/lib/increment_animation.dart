import 'package:flutter/widgets.dart';

class IncrementAnimationWidget extends StatelessWidget {
  const IncrementAnimationWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
